<h2>New Tweet!</h2>
<form data-tweets-ul = "#feed" class= "tweet-compose">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <strong class="chars-left">140</strong>

  <label>
    Content
    <textarea name="tweet[content]"></textarea>
  </label>
  <br>

  <label>
    Mention

    <a class="add-mentioned-user" href="#">Add Mention</a>
    <div class="mentioned-users">

      <script class="mention-script" type="text/template">
        <div>
          <a class="remove-mentioned-user" href="#">Remove</a>
          <select name="tweet[mentioned_user_ids][]">
            <option></option>
            <% User.all.each do |user| %>
            <option value="<%= user.id %>"><%= user.username %></option>
            <% end %>
          </select>
        </div>
      </script>
    </div>
  </label>
  <br>

  <input type="Submit" value="Post Tweet!">
</form>
